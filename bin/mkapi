#!/usr/bin/env node

// Print an error message
function error(msg) {
  var prefix = 'error: '
  console.error(prefix + msg);
  process.exit(1);
}

// Initialize the cli
var cli = require('../cli/mkapi');
cli(null, function(err) {
  if(err) {
    error(err.message || err.stack); 
  }
})
